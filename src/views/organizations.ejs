<% organizations.forEach(organization => { %>
    <div class="org-card">
        <h3>
            <a href="/organization/<%= organization.organization_id %>">
                <%= organization.name %>
            </a>
        </h3>
        <p><%= organization.description %></p>
        <!-- other fields like logo, email, etc. -->
    </div>
<% }); %>